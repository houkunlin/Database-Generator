# All text files should have the "lf" (Unix) line endings
* text eol=lf
# windows cmd should have the "crlf" (Win32) line endings
*.cmd eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jar binary
*.ttf binary
*.jks binary
*.gif binary
*.mp4 binary

